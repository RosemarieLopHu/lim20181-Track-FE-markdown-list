#!/usr/bin/env node
//Se agrega shebang characer para que sea ejcutable localmnte con node
//file System
const [,, ...args] = process.argv;
const fs = require('fs');
const marked = require('marked');

/*const fileUrl = (findFile) => {
    fs.readFile(fileUrl), */

/* exports.pruebis = pruebis;
function pruebis(name, file){
    this.name = name;
 this.writeStream = fs.createWriteStream('./' + file + '.txt',
 {'flags' : 'a',
 'encoding' : 'utf8',
 'mode' : 0666});
};

};
/* */ 
/*const fileUrl = (findFile) => {
    fs.readFile(fileUrl), */

console.log(args)




