const mdLinks = require('../app.js');

describe('verificar si mdLinks es una función', () => {
  test('verificar si es una función', () =>{
    expect(mdLinks()).toBe();

  })
  test('verificar si es una función', () =>{
    expect(mdLinks()).toBe();
  })
});

 /*  it('should be a function', () => {
    console.log('SORRY!');
  }); */


